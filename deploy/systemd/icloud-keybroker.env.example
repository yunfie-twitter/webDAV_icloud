# Copy to /etc/icloud-keybroker/keybroker.env with mode 0640.
# Select a currently vacant owner persistent handle. Never reuse or evict an
# existing handle merely to satisfy this example.
TPM_CONTEXT=0x81000003
